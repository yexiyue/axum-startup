use mongodb::Database;

#[shuttle_runtime::main]
async fn main(#[shuttle_shared_db::MongoDb] db: Database) -> shuttle_axum::ShuttleAxum {
    let router={{crate_name}}::start(db).await;
    Ok(router.into())
}
